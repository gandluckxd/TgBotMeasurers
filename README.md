# Telegram-бот для управления замерами

Telegram-бот для автоматизации процесса распределения и управления замерами с интеграцией AmoCRM.

## Возможности

### Роли пользователей

1. **Администратор**
   - Распределение новых замеров между замерщиками
   - Просмотр всех замеров и их статусов
   - Изменение назначенного замерщика
   - Управление замерщиками

2. **Замерщик**
   - Просмотр назначенных замеров
   - Изменение статуса замеров (в процессе, выполнен)
   - Получение уведомлений о новых назначениях

3. **Менеджер**
   - Отслеживание статуса замеров по своим заказам
   - Получение уведомлений о назначении замерщика
   - Получение уведомлений о завершении замеров

### Основной функционал

- **Интеграция с AmoCRM**: автоматический прием webhook при создании/изменении сделок
- **Автоматические уведомления**: все участники процесса получают уведомления о изменениях
- **Управление статусами**:
  - ⏳ Ожидает назначения
  - 📋 Назначен
  - 🔄 В процессе
  - ✅ Выполнен
  - ❌ Отменен

## Технологический стек

- **Python 3.11+**
- **aiogram 3.x** - Telegram Bot API
- **FastAPI** - веб-сервер для приема webhook
- **SQLAlchemy** - ORM для работы с БД
- **PostgreSQL/SQLite** - база данных
- **aiohttp** - асинхронные HTTP запросы
- **Loguru** - логирование

## Структура проекта

```
TgBotMeasurers/
├── bot/                      # Telegram бот
│   ├── handlers/            # Обработчики команд
│   │   ├── admin.py        # Админские команды
│   │   ├── measurer.py     # Команды замерщика
│   │   └── manager.py      # Команды менеджера
│   ├── keyboards/          # Клавиатуры
│   │   └── inline.py       # Inline кнопки
│   ├── utils/              # Утилиты
│   │   └── notifications.py # Система уведомлений
│   └── main.py             # Главный файл бота
├── server/                  # Веб-сервер
│   ├── app.py              # FastAPI приложение
│   └── webhook.py          # Обработчик webhook AmoCRM
├── database/               # База данных
│   ├── models.py           # Модели SQLAlchemy
│   └── database.py         # Управление БД
├── services/               # Внешние сервисы
│   └── amocrm.py          # Клиент AmoCRM API
├── config.py              # Конфигурация
├── main.py                # Точка входа приложения
├── requirements.txt       # Зависимости
├── .env.example          # Пример переменных окружения
└── README.md             # Документация
```

## Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd TgBotMeasurers
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните необходимые значения:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token
ADMIN_IDS=123456789,987654321

# AmoCRM
AMOCRM_SUBDOMAIN=yoursubdomain
AMOCRM_ACCESS_TOKEN=your_access_token
AMOCRM_REFRESH_TOKEN=your_refresh_token
AMOCRM_CLIENT_ID=your_client_id
AMOCRM_CLIENT_SECRET=your_client_secret
AMOCRM_REDIRECT_URI=https://yoursite.com/callback

# Webhook
WEBHOOK_HOST=https://your-domain.com
WEBHOOK_PATH=/webhook/amocrm
WEBHOOK_SECRET=your_webhook_secret_key

# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# Database
DATABASE_URL=sqlite+aiosqlite:///./measurerers_bot.db
# Для PostgreSQL:
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/measurerers_bot
```

### 5. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env` файл

### 6. Настройка AmoCRM

1. Создайте интеграцию в AmoCRM:
   - Зайдите в настройки AmoCRM
   - Раздел "Интеграции" → "Создать интеграцию"
   - Получите `CLIENT_ID` и `CLIENT_SECRET`

2. Получите токены доступа (Access Token и Refresh Token):
   - Используйте OAuth 2.0 авторизацию AmoCRM
   - Скопируйте токены в `.env`

3. Настройте webhook в AmoCRM:
   - URL: `https://your-domain.com/webhook/amocrm`
   - События: изменение статуса сделок

### 7. Запуск приложения

```bash
python main.py
```

Приложение запустит:
- Telegram бота (polling режим)
- FastAPI сервер для приема webhook на порту 8000

## Использование

### Для администратора

После запуска бота отправьте `/start` в Telegram:

**Доступные команды:**
- `/start` - Запуск бота
- `/menu` - Главное меню
- `/pending` - Новые замеры (ожидают назначения)
- `/all` - Все замеры
- `/measurers` - Список замерщиков

**Процесс работы:**
1. При получении webhook от AmoCRM, админ получает уведомление с новым замером
2. К каждому замеру прикреплены кнопки с именами замерщиков
3. Нажмите на кнопку с именем нужного замерщика для назначения
4. Замерщик и менеджер автоматически получат уведомления

### Для замерщика

**Доступные команды:**
- `/start` - Запуск бота
- `/menu` - Главное меню
- `/my` - Мои активные замеры

**Процесс работы:**
1. Получите уведомление о новом назначенном замере
2. Просмотрите детали замера
3. Нажмите "🔄 Начать выполнение" когда начинаете работу
4. После завершения нажмите "✅ Завершить"
5. Менеджер автоматически получит уведомление о завершении

### Для менеджера

**Доступные команды:**
- `/start` - Запуск бота
- `/menu` - Главное меню
- `/orders` - Мои заказы

**Уведомления:**
- Назначение замерщика на ваш заказ
- Изменение статуса замера
- Завершение замера

## API Endpoints

### Health Check

```
GET /health
```

Проверка работоспособности сервера.

### Webhook от AmoCRM

```
POST /webhook/amocrm
```

Прием webhook от AmoCRM о изменениях в сделках.

## База данных

### Модели

**User** - Пользователи бота
- `id` - Уникальный ID
- `telegram_id` - Telegram ID пользователя
- `role` - Роль (admin, measurer, manager)
- `username`, `first_name`, `last_name` - Информация о пользователе

**Measurement** - Замеры
- `id` - Уникальный ID
- `amocrm_lead_id` - ID сделки в AmoCRM
- `client_name` - Имя клиента
- `client_phone` - Телефон клиента
- `address` - Адрес замера
- `status` - Статус замера
- `measurer_id` - ID назначенного замерщика
- `manager_id` - ID менеджера
- `created_at`, `assigned_at`, `completed_at` - Временные метки

## Разработка

### Создание миграций (если используете Alembic)

```bash
alembic revision --autogenerate -m "описание изменений"
alembic upgrade head
```

### Тестирование webhook локально

Используйте [ngrok](https://ngrok.com/) для создания публичного URL:

```bash
ngrok http 8000
```

Затем используйте полученный URL в настройках webhook AmoCRM.

## Логирование

Логи сохраняются в директории `logs/`:
- `app_YYYY-MM-DD.log` - Логи приложения
- `bot_YYYY-MM-DD.log` - Логи бота

Логи ротируются ежедневно и хранятся 30 дней.

## Troubleshooting

### Бот не отвечает на команды

1. Проверьте правильность токена бота в `.env`
2. Убедитесь, что бот запущен
3. Проверьте логи в директории `logs/`

### Webhook не работает

1. Убедитесь, что FastAPI сервер запущен
2. Проверьте доступность webhook URL извне (используйте ngrok для локального тестирования)
3. Проверьте настройки webhook в AmoCRM
4. Просмотрите логи сервера

### Ошибки базы данных

1. Убедитесь, что `DATABASE_URL` правильно настроен
2. Для PostgreSQL убедитесь, что сервер запущен и доступен
3. Проверьте права доступа к файлу SQLite (если используется)

## TODO / Будущие улучшения

- [ ] Добавить возможность прикрепления фото к замеру
- [ ] Реализовать историю изменений замера
- [ ] Добавить статистику по замерщикам
- [ ] Реализовать экспорт отчетов
- [ ] Добавить возможность комментариев к замерам
- [ ] Интеграция с календарем для планирования замеров
- [ ] Уведомления по расписанию (напоминания)

## Поддержка

При возникновении проблем создайте issue в репозитории или свяжитесь с разработчиком.

## Лицензия

MIT License
